CREATE TABLE pet (
                         id INT AUTO_INCREMENT PRIMARY KEY,
                         first_name VARCHAR(30),
                         last_name VARCHAR(30),
                         pet_type VARCHAR(30),
                         pet_gender INTEGER,
                         pet_vaccination BOOLEAN
);
